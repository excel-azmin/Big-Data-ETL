rma_data:
  outputs:

    dev:
      type: postgres
      threads: 10
      host: localhost
      port: 5432
      user: postgres
      pass: postgres
      dbname: postgres
      schema: rma-server

    prod:
      type: postgres
      threads: 10
      host: localhost
      port: 5432
      user: localhost
      pass: postgres
      dbname: postgres
      schema: rma-server

  target: dev
